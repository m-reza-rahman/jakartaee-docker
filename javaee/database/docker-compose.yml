version: "3"
services:
  postgres:
    image: postgres
    container_name: JavaEEDemoDB
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./init/init.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "5432:5432"
volumes:
  pgdata:
  init: